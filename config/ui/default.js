'use strict';

module.exports = {
  ui : {
    components : require('./components'),
    fields     : require('./fields'),
    resources  : require('./resources'),
    views      : require('./views')
  }
};