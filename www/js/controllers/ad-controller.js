angular.module('app.controllers').controller('AdController', [
  '$rootScope',
  '$scope',
  '$state',
  function ($rootScope, $scope, $state) {

    $scope.init = function() {
    };

    $scope.init();
  }
]);
