angular.module('app.controllers').controller('RegistrationController', [
  '$scope',
  '$stateParams',
  function($scope, $stateParams) {
    // will mainly call through to common SignupCOntrolle perhaps
  }
]);
