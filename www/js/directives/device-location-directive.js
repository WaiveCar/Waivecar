angular.module('app.directives').directive('deviceLocation', [
  function () {
    return {
      templateUrl : '/templates/directives/device-location.html',
      scope       : {
      },
      link        : function($scope, element, attrs, ctrl) {

      }
    }
  }
]);