function CreditCardsController($rootScope, $scope) {
  var self = this;
}

angular.module('app')
.controller('CreditCardsController', [
  '$rootScope',
  '$scope',
  '$state',
  CreditCardsController
]);